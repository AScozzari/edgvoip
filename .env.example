# ============================================
# W3 VoIP System - Environment Configuration
# ============================================
# Copy this file to .env and fill in the values

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL database connection string
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=postgresql://voip_user:your_password_here@localhost:5432/voip_production

# ============================================
# AUTHENTICATION
# ============================================
# JWT Secret Key - MUST be at least 32 characters
# Generate with: openssl rand -hex 32
JWT_SECRET=your_very_long_and_secure_secret_key_here_minimum_32_characters

# ============================================
# CORS CONFIGURATION
# ============================================
# Allowed origins for CORS (comma-separated for multiple)
# Examples:
#   - Single domain: https://app.example.com
#   - Multiple domains: https://app.example.com,https://admin.example.com
#   - Development (allow all): *
CORS_ORIGIN=*

# ============================================
# FREESWITCH EVENT SOCKET (ESL)
# ============================================
# FreeSWITCH Event Socket Layer configuration for real-time call events
FREESWITCH_ESL_HOST=127.0.0.1
FREESWITCH_ESL_PORT=8021
FREESWITCH_ESL_PASSWORD=ClueCon

# ============================================
# APPLICATION SETTINGS
# ============================================
# Port for backend API server
PORT=5000

# Node environment (development | production)
NODE_ENV=production

# ============================================
# OPTIONAL: EMAIL CONFIGURATION
# ============================================
# For voicemail notifications and system alerts
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASS=your_smtp_password
# SMTP_FROM=W3 VoIP System <noreply@example.com>

# ============================================
# OPTIONAL: LOGGING
# ============================================
# Log level (error | warn | info | debug)
# LOG_LEVEL=info

# ============================================
# SECURITY NOTES
# ============================================
# 1. NEVER commit this file with real secrets to version control
# 2. Change all default passwords and secrets in production
# 3. Use strong, randomly generated values for JWT_SECRET
# 4. Restrict CORS_ORIGIN to specific domains in production
# 5. Keep FreeSWITCH ESL bound to localhost (127.0.0.1) only
# 6. Use environment-specific .env files (.env.production, .env.staging)
# 7. Rotate secrets regularly
# 8. Use a secrets management service for production (Vault, AWS Secrets Manager, etc.)
